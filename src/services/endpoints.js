const BASE_URL = "https://backdemosales-production.up.railway.app/";
const endpoints = {
  clientes: `${BASE_URL}clients`,
  vendedores: `${BASE_URL}salesmen`,
  productos: `${BASE_URL}products`,
  branches: `${BASE_URL}branches`,
  ventas: `${BASE_URL}sales`,
};

export default endpoints;
